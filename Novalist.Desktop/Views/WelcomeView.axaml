<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Novalist.Desktop.ViewModels"
             xmlns:loc="using:Novalist.Desktop.Localization"
             mc:Ignorable="d"
             d:DesignWidth="1400" d:DesignHeight="900"
             x:Class="Novalist.Desktop.Views.WelcomeView"
             x:DataType="vm:WelcomeViewModel">

    <Border Background="{DynamicResource WelcomeBackground}">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Width="820"
                            MaxWidth="820"
                            Margin="32,72,32,48"
                            Spacing="36">

                    <!-- Hero -->
                    <StackPanel Spacing="{StaticResource SpacingLarge}" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="14" HorizontalAlignment="Center">
                            <Viewbox Classes="ribbonIcon" Width="40" Height="40">
                                <Canvas Width="24" Height="24">
                                    <Path Classes="ribbonIcon" Data="{StaticResource IconBookOpen}"/>
                                </Canvas>
                            </Viewbox>
                            <TextBlock Text="{loc:Loc app.title}"
                                       FontSize="{StaticResource FontSizeDisplayLarge}"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource NormalText}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{loc:Loc app.subtitle}"
                                   FontSize="{StaticResource FontSizeBodyLarge}"
                                   Foreground="{DynamicResource SubtleText}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- Primary action row -->
                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="{StaticResource SpacingLarge}">
                        <Button Grid.Column="0"
                                Command="{Binding OpenProjectCommand}"
                                Classes="welcomeAction"
                                HorizontalAlignment="Stretch">
                            <Grid ColumnDefinitions="Auto,*">
                                <Viewbox Grid.Column="0" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <Canvas Width="24" Height="24">
                                        <Path StrokeThickness="2" StrokeLineCap="Round" StrokeJoin="Round"
                                              Stroke="{DynamicResource NormalText}"
                                              Data="{StaticResource IconFolderOpen}"/>
                                    </Canvas>
                                </Viewbox>
                                <TextBlock Grid.Column="1"
                                           Text="{loc:Loc welcome.browseFolder}"
                                           VerticalAlignment="Center" FontSize="{StaticResource FontSizeBodyLarge}"
                                           TextWrapping="Wrap" TextAlignment="Left" />
                            </Grid>
                        </Button>

                        <Button Grid.Column="1"
                                Command="{Binding ImportPluginProjectCommand}"
                                Classes="welcomeAction"
                                HorizontalAlignment="Stretch">
                            <Grid ColumnDefinitions="Auto,*">
                                <Viewbox Grid.Column="0" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,10,0">
                                    <Canvas Width="24" Height="24">
                                        <Path StrokeThickness="2" StrokeLineCap="Round" StrokeJoin="Round"
                                              Stroke="{DynamicResource NormalText}"
                                              Data="{StaticResource IconUpload}"/>
                                    </Canvas>
                                </Viewbox>
                                <TextBlock Grid.Column="1"
                                           Text="{loc:Loc welcome.importPlugin}"
                                           VerticalAlignment="Center" FontSize="{StaticResource FontSizeBodyLarge}"
                                           TextWrapping="Wrap" TextAlignment="Left" />
                            </Grid>
                        </Button>

                        <Button Grid.Column="2"
                                Command="{Binding ToggleCreateFormCommand}"
                                Classes="welcomeAction primary"
                                HorizontalAlignment="Stretch">
                            <TextBlock Text="{loc:Loc welcome.createTitle}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       FontSize="{StaticResource FontSizeBodyLarge}"
                                       TextWrapping="Wrap" TextAlignment="Center" />
                        </Button>
                    </Grid>

                    <Button Command="{Binding RunProjectWizardCommand}"
                            Classes="welcomeAction"
                            HorizontalAlignment="Stretch"
                            Margin="0,8,0,0">
                        <TextBlock Text="{loc:Loc welcome.createWithWizard}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   FontSize="{StaticResource FontSizeBodyLarge}"
                                   TextWrapping="Wrap" TextAlignment="Center" />
                    </Button>

                    <!-- Inline create form (collapsible) -->
                    <Border IsVisible="{Binding IsCreateFormOpen}"
                            Background="{DynamicResource CardBackground}"
                            BorderBrush="{DynamicResource CardBorder}"
                            BorderThickness="1"
                            CornerRadius="{StaticResource RadiusLarge}"
                            Padding="20">
                        <StackPanel Spacing="{StaticResource SpacingNormal}">
                            <StackPanel Spacing="{StaticResource SpacingTighter}">
                                <TextBlock Text="{loc:Loc welcome.projectName}" FontSize="{StaticResource FontSizeBodySmall}" Foreground="{DynamicResource DimText}" />
                                <TextBox x:Name="NewProjectNameBox"
                                         Text="{Binding NewProjectName}"
                                         Watermark="{loc:Loc welcome.projectNamePlaceholder}"
                                         MaxLength="100"
                                         FontSize="{StaticResource FontSizeBodyLarge}"
                                         KeyDown="OnCreateFormKeyDown" />
                            </StackPanel>
                            <StackPanel Spacing="{StaticResource SpacingTighter}">
                                <TextBlock Text="{loc:Loc welcome.bookName}" FontSize="{StaticResource FontSizeBodySmall}" Foreground="{DynamicResource DimText}" />
                                <TextBox Text="{Binding NewBookName}"
                                         Watermark="{loc:Loc welcome.bookNamePlaceholder}"
                                         MaxLength="100"
                                         FontSize="{StaticResource FontSizeBodyLarge}"
                                         KeyDown="OnCreateFormKeyDown" />
                                <TextBlock Text="{loc:Loc welcome.bookNameHint}" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource SubtleText}" />
                            </StackPanel>
                            <StackPanel Spacing="{StaticResource SpacingTighter}">
                                <TextBlock Text="{loc:Loc welcome.template}" FontSize="{StaticResource FontSizeBodySmall}" Foreground="{DynamicResource DimText}" />
                                <ComboBox ItemsSource="{Binding Templates}"
                                          SelectedValue="{Binding SelectedTemplateId, Mode=TwoWay}"
                                          SelectedValueBinding="{Binding Id}"
                                          HorizontalAlignment="Stretch"
                                          FontSize="{StaticResource FontSizeBodyLarge}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Spacing="{StaticResource SpacingTightest}">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="{StaticResource FontSizeBody}" />
                                                <TextBlock Text="{Binding Description}" FontSize="{StaticResource FontSizeCaption}" Foreground="{DynamicResource SubtleText}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Spacing="{StaticResource SpacingTighter}">
                                <TextBlock Text="{loc:Loc welcome.location}" FontSize="{StaticResource FontSizeBodySmall}" Foreground="{DynamicResource DimText}" />
                                <Grid ColumnDefinitions="*,Auto">
                                    <TextBox Grid.Column="0"
                                             Text="{Binding NewProjectLocation}"
                                             Watermark="{loc:Loc welcome.locationPlaceholder}"
                                             IsReadOnly="True"
                                             FontSize="{StaticResource FontSizeBodyLarge}" />
                                    <Button Grid.Column="1"
                                            Content="{loc:Loc welcome.browse}"
                                            Command="{Binding BrowseLocationCommand}"
                                            Margin="4,0,0,0"
                                            FontSize="{StaticResource FontSizeBody}" />
                                </Grid>
                            </StackPanel>
                            <Button Content="{loc:Loc welcome.createProject}"
                                    Command="{Binding CreateProjectCommand}"
                                    Classes="primary"
                                    HorizontalAlignment="Stretch"
                                    Margin="0,8,0,0" />
                        </StackPanel>
                    </Border>

                    <!-- Recent projects -->
                    <StackPanel Spacing="14">
                        <TextBlock Text="{loc:Loc welcome.recentProjects}"
                                   FontSize="{StaticResource FontSizeTitleSmall}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource NormalText}" />

                        <StackPanel HorizontalAlignment="Center"
                                    Spacing="{StaticResource SpacingMedium}"
                                    IsVisible="{Binding !RecentProjects.Count}">
                            <Viewbox Width="40" Height="40" HorizontalAlignment="Center" Opacity="0.3">
                                <Canvas Width="24" Height="24">
                                    <Path StrokeThickness="2" StrokeLineCap="Round" StrokeJoin="Round"
                                          Stroke="{DynamicResource SubtleText}"
                                          Data="{StaticResource IconBookOpen}"/>
                                </Canvas>
                            </Viewbox>
                            <TextBlock Text="{loc:Loc welcome.noRecentProjects}"
                                       FontSize="{StaticResource FontSizeBody}"
                                       Foreground="{DynamicResource SubtleText}"
                                       HorizontalAlignment="Center" />
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding RecentProjects}"
                                      IsVisible="{Binding RecentProjects.Count}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:RecentProjectCard">
                                    <Button Classes="linkButton"
                                            Command="{Binding $parent[ItemsControl].((vm:WelcomeViewModel)DataContext).OpenRecentProjectCommand}"
                                            CommandParameter="{Binding}"
                                            Padding="0"
                                            Margin="0,0,12,12">
                                        <Button.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="{loc:Loc welcome.removeFromList}"
                                                          Tag="{Binding}"
                                                          Click="OnRemoveRecentClick" />
                                            </ContextMenu>
                                        </Button.ContextMenu>
                                        <Border Background="{DynamicResource CardBackground}"
                                                BorderBrush="{DynamicResource CardBorder}"
                                                BorderThickness="1"
                                                CornerRadius="{StaticResource RadiusLarge}"
                                                Width="160"
                                                ClipToBounds="True">
                                            <StackPanel>
                                                <Panel Height="200">
                                                    <Image Source="{Binding CoverImage}"
                                                           Stretch="UniformToFill"
                                                           RenderOptions.BitmapInterpolationMode="HighQuality"
                                                           IsVisible="{Binding HasCoverImage}" />
                                                    <Border Background="{DynamicResource CardBackground}"
                                                            IsVisible="{Binding HasCoverImage, Converter={x:Static BoolConverters.Not}}">
                                                        <Viewbox Width="40" Height="40"
                                                                 HorizontalAlignment="Center" VerticalAlignment="Center">
                                                            <Canvas Width="24" Height="24">
                                                                <Path StrokeThickness="2" StrokeLineCap="Round" StrokeJoin="Round"
                                                                      Stroke="{DynamicResource SubtleText}"
                                                                      Data="{StaticResource IconBookOpen}"/>
                                                            </Canvas>
                                                        </Viewbox>
                                                    </Border>
                                                </Panel>
                                                <StackPanel Spacing="{StaticResource SpacingTightest}" Margin="10,8,10,10">
                                                    <TextBlock Text="{Binding Name}"
                                                               FontWeight="SemiBold"
                                                               FontSize="{StaticResource FontSizeBodyLarge}"
                                                               Foreground="{DynamicResource NormalText}"
                                                               TextTrimming="CharacterEllipsis"
                                                               MaxLines="1" />
                                                    <TextBlock Text="{Binding Path}"
                                                               FontSize="{StaticResource FontSizeBodySmall}"
                                                               Foreground="{DynamicResource SubtleText}"
                                                               TextTrimming="CharacterEllipsis"
                                                               MaxLines="1" />
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
